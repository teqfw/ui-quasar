/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class TeqFw_Ui_Quasar_Shared_Defaults {

    NAME = '@teqfw/ui-quasar';

    constructor() {
        Object.freeze(this);
    }
}
