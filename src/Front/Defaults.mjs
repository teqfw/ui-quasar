/**
 * Plugin constants (hardcoded configuration) for frontend code.
 */
export default class TeqFw_Ui_Quasar_Front_Defaults {

    DI_QUASAR = 'quasarLib'; // DI container key for Quasar lib.

    constructor() {
        Object.freeze(this);
    }
}
